# YOLOv5 🚀 by Ultralytics, AGPL-3.0 license
# 
# Example usage: python train.py --img1280 --batch8 --epochs 100 --data '/data/custom_data.yaml' --cfg 'models/yolov5s.yaml' --weights yolov5s.pt --device 0 s--name 1st_train_0 --resume train_exp8_best.pt

# parent
# ├── yolov5
# └── data
#     └── images  
#     └── labels  

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]

path: ./data # dataset root dir
train: ../2nd_train # train images (relative to 'path') 
val: ../2nd_valid # val images (relative to 'path') 
# test: /data/images/1st_test # test images (optional)

# number of classes
nc: 4

# Classes
names:
  0: hyoid_bone
  1: neck_bone
  2: food_locate
  3: food